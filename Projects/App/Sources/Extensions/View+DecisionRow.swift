import SwiftUI
import ZohoBooksClient
import BookkeeperCore

extension View {
    func decisionRow() -> some View {
        self
            .padding(.horizontal)
            .padding(.vertical, 12)
    }
}
